'use strict';

const CONFIG = Object.freeze({
  strategyMode: 'LONG_ONLY',
  timezone: 'Asia/Taipei',
  initialCapital: 100000,
  rawVolumeReviewLimit: 0,
  candidateSelectionPoolLimit: 50,
  coreLiquidityLimit: 40,
  emergingLiquidityLimit: 10,
  motherPoolMinActiveDays: 18,
  topVolumeLimit: 10,
  maxCandidates: 10,
  maxOpenPositions: 5,
  candidateRankingIntervalMinutes: 60,
  // Top50 只作為高流動性的可交易母池；Top10 觀察排序由三大面向決定。
  candidateSelectionWeights: Object.freeze({ chip: 0.50, technical: 0.30, liquidity: 0.20 }),
  schedulerIntervalMinutes: 5,
  sessionStart: '08:50',
  tradingStart: '09:10',
  dayTradeEntryCutoff: '12:30',
  overnightEntryStart: '13:00',
  forcedExitStart: '13:20',
  marketClose: '13:30',
  sessionEnd: '13:20',
  strategyCaps: Object.freeze({ SWING: 0.50, OVERNIGHT: 0.30, DAY_TRADE: 0.15 }),
  minCashReservePct: 0.30,
  dailyNewCapitalPct: 0.20,
  dailyNewCapitalLimit: 300000,
  dailyTurnoverPct: 0.40,
  firstEntryPct: 0.10,
  addOnPct: 0.05,
  maxSymbolPct: 0.15,
  settlementReservePct: 0.05,
  settlementReserveMin: 5000,
  dailyStopPct: -0.02,
  weeklyStopPct: -0.05,
  brokerFeeRate: 0.001425,
  minBrokerFee: 1,
  sellTaxRate: 0.003,
  dayTradeTaxRate: 0.0015,
  maxReprices: 3,
  maxChasePct: 0.003,
  maxSpreadPct: 0.006,
  quoteMaxAgeMs: 10 * 60 * 1000,
  liveScoreConcurrency: 6,
  scoreThresholds: Object.freeze({ A: 80, B: 75, C: 50 })
});

module.exports = { CONFIG };
